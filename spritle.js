//a:und

// console.log(a, b, c);

// let a = "hellow";

let fullname = (first, second) => {
  console.log(`${first} ${second}`);
};

name = (first, second, fullname) => {

    //cal

  fullname(first, second);
};

name("siva", "sakthi", fullname);

//arrow
//let const
//destruction
